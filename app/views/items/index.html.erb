<h1>購入商品一覧</h1>
<% if %>
    <% @items.each do |item| %>
        <p><%= item.name %> : <%= item.price %> : <%= item.user.nickname.present? ? "購入者 #{item.user.nickname}" : "会員No.#{item.user_id}" %></p>
    <% end %>
<% else %>
    <p>ログインしてください</p>
<% end %>
